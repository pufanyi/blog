Describe the concrete problem and resulting behavior. Include relevant validation and limitations.

- [ ] Updated the handbook page in `docs/` (see `docs/documentation.md`), or explained why the documented behavior is unchanged.
- [ ] Ran `pnpm check` and `pnpm test --watch=false` after the final change.
- [ ] Ran relevant production browser checks for rendering, navigation, interaction, or hosting changes.

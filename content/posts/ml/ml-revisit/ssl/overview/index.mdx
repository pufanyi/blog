---
title: "Self-Supervised Learning：从哪里得到监督信号？"
date: "2026-09-12"
description: "视觉 SSL 系列导读：从预测目标、避免 collapse 的机制和下游评估，串起 contrastive learning、self-distillation、masked modeling 与 JEPA。"
---

给一张照片标上「猫」，是人在提供监督信号。但即使没有这个标签，我们仍然知道：同一张照片的两个裁剪来自同一个场景，被遮住的区域与周围内容有关，一段视频的前后帧通常描述同一个过程。**Self-supervised learning 利用数据内部的关系构造训练任务，再把学到的表征用于其他任务。**

这里的 SSL 指 self-supervised learning。这个缩写也常用于 semi-supervised learning，后者通常指同时利用有标签与无标签数据。这组文章主要讨论视觉表征；语言模型的 next-token prediction 也属于自监督任务，但不在这里展开。

## 先分清训练任务和真正想做的任务

设图像为 $x$，encoder 为 $f_\theta$，我们希望得到有用的表征 $h=f_\theta(x)$。预训练时，模型可能在判断两个裁剪是否来自同一张图，也可能在预测被遮住的 patch。部署时，我们却可能用 $h$ 做分类、检索、分割，甚至接入一个控制系统。

因此，**把预训练任务做得好，不等于学到了适合下游任务的表征**。例如，两个裁剪的颜色分布很相似，模型可能只比较颜色就完成匹配，却没有理解形状。反过来，如果任务需要识别物体颜色，过强的 color jitter 又可能丢掉有用信息。数据增强实际上在告诉模型：哪些变化应该被忽略，哪些关系应该被保留。

Stanford CS231n 2026 的 Lecture 12 [@cs231n2026ssl] 从 pretext task、contrastive learning 走到 masked prediction，适合建立这张地图。下面按机制组织，而不把每篇论文当作互不相关的新算法。

## 两个问题串起不同方法

第一个问题是：**目标是什么？** 可以是另一张图的 embedding、对一组 prototype 的概率分布、离散 visual token、原始像素，或者目标区域的连续特征。

第二个问题是：**为什么模型不能投机取巧？** 假设只要求同一张图的两个 view 接近：

$$
\mathcal L_{\mathrm{align}}
=\mathbb E_{x,t_1,t_2}
\left\|f_\theta(t_1(x))-f_\theta(t_2(x))\right\|_2^2.
$$

令所有输入都输出同一个常向量，loss 就是零，但表征已经无法区分任何图像。这叫 complete collapse。还有一种更隐蔽的 dimensional collapse：输出仍然不同，却主要挤在少数几个方向上。

<SSLMap />

图中的类别可以交叉。例如，DINOv2 同时用图像级 self-distillation 和 patch 级 masked prediction；JEPA 也会 mask 输入，但预测的是学习到的特征。**Masking 描述输入怎样被隐藏，distillation 描述目标怎样产生，两者不是互斥的分类。**

## 阅读路线

| 文章 | 核心问题 | 主要方法 |
| --- | --- | --- |
| [Contrastive learning](/blog/ml/ml-revisit/ssl/contrastive) | 从许多候选里找出另一份 view，候选集合怎样构造？ | SimCLR、MoCo v1/v2/v3 |
| [不用负样本的表征学习](/blog/ml/ml-revisit/ssl/non-contrastive) | 只让两个 view 接近，怎样避免 collapse？ | BYOL、SimSiam、Barlow Twins、VICReg |
| [从聚类到 self-distillation](/blog/ml/ml-revisit/ssl/clustering-distillation) | 能否把匹配对象从单个样本换成共享的预测目标？ | SwAV、DINO、iBOT、DINOv2/v3 |
| [Masked image modeling](/blog/ml/ml-revisit/ssl/masked-modeling) | 遮住一块图像以后，应该预测什么？ | MAE、BEiT、BEiT v2 |
| [JEPA：在表征空间里预测](/blog/ml/ml-revisit/ssl/jepa) | 能否跳过难以预测的像素细节？ | I-JEPA、V-JEPA、V-JEPA 2/2.1 |
| [LeJEPA 与 SIGReg](/blog/ml/ml-revisit/ssl/lejepa) | 能否直接规定表征分布，让避免 collapse 的目标更明确？ | LeJEPA、随机投影、Gaussian matching |

第一次读，建议从 contrastive learning 顺着走到 DINO，再比较 MAE 和 JEPA。想理解 LeJEPA，先读 VICReg，再读 JEPA；前者解释分布约束，后者解释为什么在特征空间构造预测任务。

## 怎样判断表征有用

UW 的 Deep Learning 2026 Lecture 15 [@uw2026ssl] 把流程明确分成 pretext training 和 transfer：前一步构造自监督任务，后一步检查特征能否支持真正需要的任务。因此，看到论文中的 accuracy，要先看评估时允许训练哪些参数。

| 评估方式 | 允许学习什么 | 主要观察什么 |
| --- | --- | --- |
| $k$-NN | 冻结 encoder，用带标签的参考集投票 | 特征空间的距离是否有意义 |
| Linear probe | 冻结 encoder，只训练线性分类器 | 信息是否容易被线性读出 |
| Fine-tuning | 更新 encoder 和任务 head | 预训练是否提供了好的初始化 |
| Dense prediction | 接入分割、检测、深度等任务，并注明是否冻结 | 局部位置与几何信息是否保留下来 |

同一个模型，linear probe 普通而 fine-tuning 很强，并不矛盾。一个强调图像级语义的模型，也可能不擅长定位边界。比较不同方法时，还需要对齐 backbone、预训练数据、分辨率、训练预算和 downstream protocol。SSL Cookbook [@balestriero2023cookbook] 对这些训练与评估选择有系统讨论。

还要区分「预训练目标没有人工标签」和「整个开发过程没有用过标签」。用有标签的验证集挑选 checkpoint、训练 linear probe，或者用已有模型筛选数据，都是需要交代的实验条件。

## 课程与教程怎么配合读

这组文章的公式和方法归属会引用原始论文；课程与教程主要用于串起动机和实现。下面几份材料可以按需要搭配：

| 材料 | 适合解决的问题 |
| --- | --- |
| Stanford CS231n，Spring 2026，Lecture 12 [@cs231n2026ssl] | 从传统 pretext task 到现代视觉 SSL 的整体脉络 |
| University of Washington CSE 493G1/599G1，Winter 2026，Lecture 15 [@uw2026ssl] | 对照不同监督方式、视觉 pretext task 与表征评估 |
| UvA DL Notebooks，Tutorial 17 [@lippeSimCLR] | 把 SimCLR 的增强、loss 和 linear evaluation 写成可运行代码 |
| *A Cookbook of Self-Supervised Learning* [@balestriero2023cookbook] | 系统查阅方法差异、训练配方与评估细节 |

近期方法部分按截至 2026 年 9 月 12 日可核实的原始论文与作者实现整理；课程里的经典框架和后续研究进展在各篇中分别说明。
